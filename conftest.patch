--- tests/conftest.py.orig	2025-07-15 12:20:34
+++ tests/conftest.py	2025-07-15 12:22:58
@@ -165,6 +165,9 @@
 
 
 def pytest_sessionstart(session: Session) -> None:  # noqa: U100
+    if os.environ.get("MUTANT_UNDER_TEST"):
+        return
+
     if get_xdist_worker_id(session) != "master":
         return
 
@@ -197,6 +200,8 @@
     session: Session, exitstatus: int  # noqa: U100
 ) -> None:
     del exitstatus
+    if os.environ.get("MUTANT_UNDER_TEST"):
+        return
     if get_xdist_worker_id(session) != "master":
         return
 
